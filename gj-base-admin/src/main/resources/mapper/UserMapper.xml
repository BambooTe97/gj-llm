<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gj.llm.admin.mapper.UserMapper">

    <!-- 结果映射：用户基础字段 -->
    <resultMap id="BaseResultMap" type="com.gj.llm.base.entity.UserEntity">
        <id     column="id"         property="id"/>
        <result column="username"   property="username"/>
        <result column="password"   property="password"/>
        <result column="nickname"   property="nickname"/>
        <result column="avatar"     property="avatar"/>
        <result column="email"      property="email"/>
        <result column="status"     property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, avatar, email, status, created_at, updated_at
        FROM sys_user
        WHERE username = #{username}
    </select>

    <!-- 统计用户名数量（唯一性校验） -->
    <select id="countByUsername" resultType="int">
        SELECT COUNT(1) FROM sys_user WHERE username = #{username}
    </select>

</mapper>
