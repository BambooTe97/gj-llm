<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gj.llm.rag.mapper.DatasetMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.gj.llm.rag.entity.DatasetEntity">
        <id     column="id"               property="id" />
        <result column="name"             property="name" />
        <result column="description"      property="description" />
        <result column="embedding_model"  property="embeddingModel" />
        <result column="vector_store_type" property="vectorStoreType" />
        <result column="collection_name"  property="collectionName" />
        <result column="chunk_size"       property="chunkSize" />
        <result column="chunk_overlap"    property="chunkOverlap" />
        <result column="status"           property="status" />
        <result column="doc_count"        property="docCount" />
        <result column="segment_count"    property="segmentCount" />
        <result column="created_at"       property="createdAt" />
        <result column="updated_at"       property="updatedAt" />
    </resultMap>

    <!-- 通用查询列 -->
    <sql id="Base_Column_List">
        id, name, description, embedding_model, vector_store_type, collection_name,
        chunk_size, chunk_overlap, status, doc_count, segment_count, created_at, updated_at
    </sql>

</mapper>
